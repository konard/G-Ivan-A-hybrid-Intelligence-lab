---
status: draft
version: 0.3
updated: 2026-07-17
temperature: 0.7
type: research
context: [retrieval, rag, education, theory, conceptual-framing, object-model, hypotheses]
method: literature-survey + adversarial-hypotheses + taxonomy-building
scope: repo-wide
source: "https://github.com/G-Ivan-A/hybrid-Intelligence-lab/issues/437"
based_on:
  - research/education/2026-07-16-retrieval-strategies-survey.md
  - research/hub/2026-07-04-hub-as-agent-system-global-analysis.md
  - docs/vision.md
related_artifacts:
  - "research/ai-education/retrieval/00-introduction.md"
  - "research/ai-education/retrieval/20-taxonomy.md"
  - "research/ai-education/retrieval/30-decision-framework.md"
  - "research/ai-education/retrieval/40-practice-and-cases.md"
  - "research/ai-education/retrieval/50-open-research.md"
  - "docs/rfc/2026-07-17-rfc-reference-research-pattern.md"
  - "research/education/2026-07-16-retrieval-strategies-survey.md"
related_issues:
  - "https://github.com/G-Ivan-A/hybrid-Intelligence-lab/issues/437"
  - "https://github.com/G-Ivan-A/hybrid-Intelligence-lab/issues/418"
---

# Retrieval: теория — Conceptual Framing, объектная модель, точки решений, гипотезы

> **Модуль.** Файл `10-theory.md` модуля `research/ai-education/retrieval/`
> ([карта чтения](00-introduction.md#карта-чтения)). Это **устойчивая ветка**:
> она меняется медленно и служит основанием для
> [30-decision-framework.md](30-decision-framework.md) и
> [40-practice-and-cases.md](40-practice-and-cases.md).

## 1. Conceptual Framing

**Conceptual Framing** — рамка, в которой поле retrieval становится обсуждаемым:
какими объектами оно описывается, что в нём является решением, а что —
следствием. Термин выбран сознательно вместо «Mental Model»: рамка здесь —
**проверяемая конструкция исследования** (её элементы опровергаются гипотезами
§5 и самоаудитом [Открытые вопросы §6](50-open-research.md#6-самоаудит)), а не
удобная картинка в голове читателя. Mental Model допускает «мне так удобнее
думать»; Conceptual Framing обязан отвечать на вопрос «а что сделает это
утверждение ложным».

### 1.1. Формальная сигнатура

Всё исследование сводится к одной функции:

```
S = Decision(KB, Query, Constraints)
```

| Переменная | Читается | Расшифровка | Где раскрывается |
| --- | --- | --- | --- |
| **S** | Strategy | Выбранная retrieval-стратегия: композиция ретривера, реранкера, генератора и оценщика с политикой ветвления (сущность E11, §2). Область значений — S0–S6: [Таксономия §3](20-taxonomy.md#3-таксономия-retrieval-стратегий) | §2 (E11), [Таксономия §3](20-taxonomy.md#3-таксономия-retrieval-стратегий) |
| **KB** | Knowledge Base | Профиль базы знаний: шесть ортогональных осей K1–K6 (объём, структурность, волатильность, связность, модальность, согласованность) плюс уровень готовности L0–L5 | [Таксономия §1](20-taxonomy.md#1-таксономия-базы-знаний), [Решения §6](30-decision-framework.md#6-readiness-levels-бз-l0l5) |
| **Query** | Query | Класс информационной потребности (Q1–Q10), а не буквальный текст пользователя: между текстом и Query стоит переписывание (§4, D3) | [Таксономия §2](20-taxonomy.md#2-таксономия-запросов) |
| **Constraints** | Constraints | Внешние ограничения, не выводимые из KB и Query: бюджет и латентность, требования к объяснимости и правам доступа, зрелость команды, регуляторика. Именно они делают «лучшую по метрике» стратегию неприменимой | [Открытые вопросы §1](50-open-research.md#1-зависимости-выбора-стратегии), модификаторы M1–M3 в [Решения §1.3](30-decision-framework.md#13-модификаторы-третьепятое-измерения) |
| **Decision** | Decision | **Функция человека, а не системы** (Rule 4, [AI_GOVERNANCE.md](../../../ai-governance/ai-governance.md)). Матрица [Решения §1](30-decision-framework.md#1-матрица-бз--запрос--стратегия-ядро-исследования) — её табличная аппроксимация: инструмент выбора для человека, а не автомат выбора за человека | §4 (D1–D9), [Решения §1](30-decision-framework.md#1-матрица-бз--запрос--стратегия-ядро-исследования) |

**Что даёт сигнатура.** Три следствия, каждое из которых проверяемо:

1. **Стратегия — не свойство инструмента, а функция входа.** Утверждения вида
   «RAG лучше» некорректны по типу: у `Decision` нет аргумента «мода».
   Отсюда вердикты по H1 и H4 (§5).
2. **Пропуск аргумента — это дефект, а не упрощение.** Типовой провал внедрений —
   `Decision(KB, Query)` без `Constraints`: стратегия выбрана по метрике и
   не выживает в эксплуатации ([Практика §1](40-practice-and-cases.md#1-индустриальные-кейсы)).
3. **Каждый аргумент обязан быть измеримым.** Поэтому у каждой оси таксономии
   есть наблюдаемый признак, а у готовности БЗ — шкала L0–L5: неизмеримый
   аргумент превращает `Decision` в вкусовщину.

### 1.2. Границы рамки

Сигнатура **не** утверждает, что `Decision` детерминирована: при одинаковых
`(KB, Query, Constraints)` два инженера могут обоснованно выбрать разные S —
матрица даёт **класс** допустимых стратегий, а не точку. Сигнатура также не
охватывает обучение системы во времени: `Decision` описывает выбор в момент t,
а не дрейф БЗ и модели (это открытые вопросы,
[Открытые вопросы §1](50-open-research.md#1-зависимости-выбора-стратегии)).

## 2. Object model

Десять сущностей поля. Модель сознательно **отделяет роль от реализации**: одна
и та же библиотека может исполнять несколько ролей, но роли не сливаются.

| # | Сущность | Определение (роль, не реализация) | Ключевые атрибуты | Чем **не** является |
| --- | --- | --- | --- | --- |
| E1 | **Knowledge Base (БЗ)** | Совокупность документов-источников до всякой обработки | объём, структурность, волатильность, связность, модальность, наличие противоречий | не индекс; БЗ существует до и независимо от retrieval |
| E2 | **Document** | Единица источника с собственной идентичностью и версией | id, версия, дата, тип, права доступа | не чанк |
| E3 | **Chunk** | Единица индексации и извлечения | текст, границы, родитель, метаданные, позиция | не абзац; граница чанка — *решение*, а не свойство текста |
| E4 | **Embedding** | Отображение чанка (или запроса) в вектор | модель, размерность, нормировка, домен обучения | не «смысл»; это координаты в пространстве конкретной модели |
| E5 | **Index** | Структура, отвечающая на запрос «ближайшие кандидаты» | тип (dense/sparse/graph/hybrid), метрика, параметры ANN, свежесть | не БД; БД — реализация |
| E6 | **Query** | Формулировка информационной потребности | тип, длина, специфичность, наличие сущностей, hop-число | не «вопрос пользователя»: между ними стоит переписывание (§4, D3) |
| E7 | **Retriever** | Оператор `Query → список кандидатов` | стратегия, k, фильтры | не Reranker: retriever оптимизирует *recall*, не precision |
| E8 | **Reranker** | Оператор `(Query, кандидаты) → упорядоченные кандидаты` | модель (cross-encoder / LLM), глубина, стоимость | не Retriever: не может вернуть то, чего нет в кандидатах |
| E9 | **Generator** | Оператор `(Query, контекст) → ответ` | модель, окно, промпт, политика отказа | не Retriever; провал генерации ≠ провал retrieval (RQ28) |
| E10 | **Evaluator** | Оператор `(Query, контекст, ответ, эталон?) → метрики` | метрики, тип эталона, судья | не мониторинг; eval — оффлайн-контур |

**Сущность-модификатор (не объект, а ограничение):**

| # | Сущность | Роль |
| --- | --- | --- |
| E11 | **Strategy** | Композиция E7–E10 с политикой ветвления. Именно Strategy — то, что выбирает человек в матрице [Решения §1](30-decision-framework.md#1-матрица-бз--запрос--стратегия-ядро-исследования); всё остальное — её части. |

**Почему это важно педагогически.** Типовая ошибка новичка — считать, что
«retriever плохой», когда на деле неверна **граница чанка** (E3) или ложна
**геометрия эмбеддинга** (E4, §5 H4). Объектная модель делает диагноз
адресуемым: у каждого симптома есть сущность-подозреваемый (§4).

---

## 3. Dependency graph

### 3.1. Поток исполнения (runtime)

```
                       ┌──────────────────────────────────────────┐
                       │  D1: нужен ли retrieval вообще?          │
                       └────────────────┬─────────────────────────┘
                                        │ да
  User intent ──► [E6 Query] ──D3──► Query rewrite / expansion
                                        │
                    ┌───────────────────┼───────────────────┐
                    │ D4: фильтр по метаданным              │
                    ▼                                       ▼
              [E5 Index: dense]                       [E5 Index: sparse/BM25]
                    │                                       │
                    └──────────────► D5: fusion (RRF) ◄─────┘
                                        │
                                 [E7 Retriever] → top-k кандидатов
                                        │
                                D6: reranking? ──► [E8 Reranker] → top-n
                                        │
                        D7: достаточен ли контекст?  ──нет──► петля (CRAG/Self-RAG)
                                        │ да
                                 [E9 Generator] ──► ответ
                                        │
                                 [E10 Evaluator]  (оффлайн-контур)
```

### 3.2. Поток подготовки (build-time)

```
[E1 KB] ──D8: readiness ([Решения §6](30-decision-framework.md#6-readiness-levels-бз-l0l5))──► [E2 Document] ──D2: chunking──► [E3 Chunk]
                                                                     │
                                       D9: обогащение контекстом ────┤
                                       (Contextual Retrieval)        │
                                                                     ▼
                                                          [E4 Embedding] ──► [E5 Index]
```

### 3.3. Критические зависимости (что от чего ломается)

| Зависимость | Механизм | Симптом при нарушении |
| --- | --- | --- |
| E3 → E4 | размер чанка задаёт, что *попадёт* в один вектор | чанк 2000 токенов усредняет несколько тем → вектор не близок ни к одному запросу («размытие темы») |
| E4 → E5 | метрика индекса должна соответствовать обучению модели | модель обучена на dot product, индекс считает cosine на ненормированных векторах → тихая деградация без ошибки |
| E7 → E8 | reranker видит только то, что вернул retriever | идеальный reranker + recall@k=0.4 ⇒ потолок качества 0.4 |
| E3 → E9 | чанк — единица контекста генератора | мелкий чанк проходит поиск, но лишён контекста → верный retrieval, неверный ответ (RQ16) |
| E1 → всё | противоречия/дубликаты в корпусе | «правильных» ответов два, оба извлечены → генератор выбирает произвольно (RQ19) |
| E6 → E7 | рассинхрон формулировки и корпуса | запрос на языке пользователя, корпус на языке документа → провал даже при идеальном индексе |

**Свойство графа, важное для решений:** зависимости **однонаправлены вниз по
качеству**. Ошибка на входе (E1, E3) не компенсируется мощностью на выходе
(E8, E9) — это прямое следствие строки «E7 → E8» (потолок recall). Отсюда
приоритет readiness-анализа ([Решения §6](30-decision-framework.md#6-readiness-levels-бз-l0l5)) над выбором модели.

---

## 4. Decision points

Девять точек, где принимается решение — человеком (H), агентом (A) или **по
умолчанию библиотеки** (D — «решение, которое никто не принимал»). Колонка
«Обычно» фиксирует, кем решение принимается на практике, колонка «Должно» — кем
по итогам этого исследования.

| # | Точка решения | Варианты | Обычно | Должно | Цена ошибки |
| --- | --- | --- | --- | --- | --- |
| D1 | **Нужен ли retrieval** для этого запроса | нет / Golden Set / retrieval | D (всегда retrieval) | A (Self-RAG) под политикой H | лишняя латентность + шум в контексте |
| D2 | **Границы чанка** | fixed / recursive / semantic / document-aware / parent-child | D (recursive 1000/200 по умолчанию LangChain) | H на основе структуры корпуса | потолок качества всего конвейера |
| D3 | **Переписывание запроса** | как есть / rewrite / expansion / HyDE / decomposition | D (как есть) | A под политикой H | провал на multi-hop и на жаргоне |
| D4 | **Фильтр по метаданным** | нет / жёсткий / мягкий (буст) | D (нет) | H (политика доступа — всегда H) | жёсткий фильтр отсекает верный ответ |
| D5 | **Слияние dense+sparse** | только dense / только sparse / RRF / взвешенная сумма | D (только dense) | H | систематический провал на артикулах/кодах (RQ21) |
| D6 | **Reranking** | нет / cross-encoder / LLM-судья | D (нет) | H по бюджету | precision при большом k |
| D7 | **Достаточность контекста** | принять / переспросить / отказаться | D (всегда принять) | A (CRAG) под политикой H | галлюцинация на пустом контексте |
| D8 | **Готовность БЗ** | индексировать / чинить корпус / не векторизовать | D (индексировать всё) | **H, всегда** | необъяснимая деградация без диагноза |
| D9 | **Политика отказа генератора** | всегда отвечать / отказ при низкой уверенности | D (всегда отвечать) | H | Hallucinated-confidence (§15.1.9 v0.4) |

**Наблюдение (В3).** Семь из девяти точек в типовом внедрении заняты
умолчаниями библиотеки. Это делает систему **необъяснимой**: при деградации
непонятно, какое из семи молчаливых решений отказало. Отсюда норма, которую
исследование предлагает на вход RFC-F: *решение, принятое умолчанием, должно
быть либо явно принято, либо явно помечено как принятое умолчанием* — прямая
проекция инварианта «Человек > Команда Q > Исполнитель»
([AI_GOVERNANCE.md](../../../ai-governance/ai-governance.md), §2.2 v0.4) на retrieval-конвейер.

---

## 5. Провокационные гипотезы (adversarial triggers)

Гипотезы — **точки входа в анализ**, не тезисы для подтверждения. Вердикты:
✅ держит, ⚠️ держит с переформулировкой, ❌ не держит, ⏳ вердикт на Этапе 2.
Итоговое ранжирование по «несущей способности» — [Открытые вопросы §3](50-open-research.md#3-ранжирование-гипотез-какие-несут-нагрузку) (Этап 3).

### 5.1. Гипотезы фаундера (H1–H7)

#### H1. «RAG целесообразен при определённом типе запросов и структуре БЗ» — ⏳ (вердикт Э2)

**Точка входа.** Гипотеза верна почти тавтологически; продуктивный вопрос —
*какие именно* типы и структуры, и **чем измеряется** «целесообразен».
Adaptive-RAG (Jeong et al., NAACL 2024,
[arXiv:2403.14403](https://arxiv.org/abs/2403.14403)) даёт эмпирическое
подтверждение самой посылки: авторы строят классификатор сложности запроса
именно потому, что единая стратегия проигрывает по совокупности
«качество × стоимость». Обратная сторона: гипотеза не задаёт **порогов**.
→ Развёртывается в [Таксономия §2](20-taxonomy.md#2-таксономия-запросов) (таксономия запросов), [Таксономия §1](20-taxonomy.md#1-таксономия-базы-знаний) (таксономия БЗ), [Решения §1](30-decision-framework.md#1-матрица-бз--запрос--стратегия-ядро-исследования) (матрица).

#### H2. «Для чата поддержки — Golden Set, эскалация в RAG/оператор» — ⚠️ держит с переформулировкой

**Точка входа.** Формулировка «Golden Set вместо RAG» смешивает два разных
объекта: Golden Set как **кэш ответов** (runtime) и golden set как **эталонный
набор для оценки** (eval, §15.1.6 v0.4). Это разные сущности с одинаковым
именем — терминологическая ловушка, которую образовательный модуль обязан снять.

**Переформулировка.** Речь идёт о **каскаде с ранним выходом**:
`точное совпадение → семантическое совпадение с высоким порогом → RAG → оператор`.
Это не «вместо RAG», а *маршрутизатор*, вырожденный случай Adaptive RAG
(Jeong et al., 2024), где классификатор заменён порогом уверенности.

**Что гипотеза упускает.** Цену **ложного срабатывания** кэша: близкий по форме,
но иной по смыслу вопрос («как отменить заказ» vs «как отменить *подписку*»)
получает уверенно-неверный FAQ-ответ. Порог — не гиперпараметр, а **решение о
цене ошибки** (D9). → [Решения §1](30-decision-framework.md#1-матрица-бз--запрос--стратегия-ядро-исследования), [Решения §6](30-decision-framework.md#6-readiness-levels-бз-l0l5).

#### H3. «Для поиска зависимостей — RAG + Reranking» — ⏳ (вердикт Э2)

**Точка входа.** Гипотеза сама себя опровергает по механике: reranking
переупорядочивает то, что вернул retriever (E7 → E8, §3.3), а зависимость
(«что сломается, если изменить X») **не содержится в одном чанке** — она есть
свойство *графа связей*, а не текста. Ни retriever, ни reranker не выводят
транзитивное замыкание. Это территория Graph RAG (Edge et al., 2024,
[arXiv:2404.16130](https://arxiv.org/abs/2404.16130)), где ответ строится по
структуре сущностей и связей. → [Таксономия §3](20-taxonomy.md#3-таксономия-retrieval-стратегий), [Решения §1](30-decision-framework.md#1-матрица-бз--запрос--стратегия-ядро-исследования).

#### H4. «Косинусный поиск улучшает семантический поиск» — ❌ не держит

**Опровержение — не риторическое, а по источнику.** Косинус не «улучшает
семантический поиск»: он **является** одним из способов его реализовать, то есть
допущением о том, что смысловая близость = угловая близость в пространстве
конкретной модели. Steck, Ekanadham & Kallus (WWW '24 Companion,
[arXiv:2403.05440](https://arxiv.org/abs/2403.05440), «Is Cosine-Similarity of
Embeddings Really About Similarity?») показывают аналитически: для выученных
эмбеддингов косинус может давать **произвольные и потому бессмысленные**
значения сходства — эффект зависит от регуляризации, а не от семантики.

**Продуктивная переформулировка (когда косинус работает плохо):**
1. Когда модель обучалась под **dot product**, а нормировка при индексации
   отбрасывает норму вектора — а норма несёт сигнал (например, «уверенность»
   или частотность).
2. На **редких/точных токенах** (артикул, код ошибки, номер версии): плотный
   вектор их усредняет — систематический проигрыш BM25 (RQ21).
3. На **длинных чанках**: усреднение нескольких тем в один вектор (§3.3, E3→E4).
4. При **асимметрии** «короткий запрос ↔ длинный документ» — задача, под которую
   bi-encoder не обучался, если не применялась соответствующая процедура
   (ср. DPR: Karpukhin et al., EMNLP 2020,
   [arXiv:2004.04906](https://arxiv.org/abs/2004.04906)).

**Вывод для образования.** Косинус — базовый механизм с явным допущением, а не
улучшение. Правильный вопрос обучаемому: «какое допущение о геометрии вы
приняли и когда оно ложно».

#### H5. «Готовность БЗ к векторизации» — ⏳ (вердикт Э2, конструктивный)

**Точка входа.** Гипотеза *не проверяема* в текущей формулировке — нет шкалы.
Её ценность в том, что она указывает на **реальный пробел поля**: бенчмарки
меряют методы на фиксированных корпусах (BEIR: Thakur et al., NeurIPS 2021),
но не меряют **корпус как вход**. → [Решения §6](30-decision-framework.md#6-readiness-levels-бз-l0l5) (L0–L5) — оригинальный вклад.

#### H6. «Стратегия разбиения на чанки (200 vs 2000); что для price-листов?» — ⏳ (вердикт Э2)

**Точка входа.** Числа 200/2000 — не теория, а диапазон умолчаний
инструментов. Теоретическая основа существует, но она **не про число токенов**,
а про соотношение «одна смысловая единица = один вектор» (§3.3). Для
price-листов гипотеза упирается в вопрос иной природы: строка прайса — это
**структурированная запись**, и корректный ответ может состоять не в улучшении
чанкинга, а в отказе от retrieval в пользу запроса к структурированному
хранилищу. → [Решения §2](30-decision-framework.md#2-chunking), RQ15.

#### H7. «ДНК-метафора» — ⚠️ держит как педагогический инструмент, с явными границами

`chunk = фрагмент ДНК`, `query = probe`, `embedding = sequencing`,
`similarity search = гибридизация`, `reranking = ПЦР-амплификация`.

**Что метафора объясняет хорошо** ([Практика §4](40-practice-and-cases.md#4-днк-метафора-где-работает-и-где-ломается), Этап 3 — развёрнуто): гибридизация
зонда с комплементарной последовательностью — сильный образ для *semantic match*
против *точного совпадения строки*; «строгость отжига» (stringency) — прямой
аналог порога similarity.

**Где метафора ломается (границы обязательны, иначе она вредна):**
- ПЦР **амплифицирует**, но не **переупорядочивает**; reranking же меняет
  порядок, ничего не добавляя. Аналогия неверна по существу операции —
  ближе была бы «селекция/скрининг клонов», а не амплификация.
- Гибридизация ДНК основана на **детерминированной комплементарности**;
  semantic similarity — **выученная и оспариваемая** метрика (H4). Метафора
  внушает ложную объективность.

Метафора принимается **как вход в тему**, а не как модель, и обязана
сопровождаться разделом «где она ломается» — иначе воспроизводит
Hallucinated-confidence (§15.1.9 v0.4).

### 5.2. Гипотезы, добавленные исполнителем из международных практик (H8–H20)

Расширение обязательно по issue #418 («Исполнитель должен расширить этот
список»). Каждая — с источником-триггером.

| # | Гипотеза (провокационная формулировка) | Источник-триггер | Почему это adversarial trigger | Этап |
| --- | --- | --- | --- | --- |
| H8 | «Длинный контекст отменяет RAG» | Li et al., 2024, [arXiv:2407.16833](https://arxiv.org/abs/2407.16833); Liu et al., TACL 2024, [arXiv:2307.03172](https://arxiv.org/abs/2307.03172) | Если модель читает миллион токенов, зачем индекс? Контр-довод — «lost in the middle» + стоимость + свежесть | Э3 |
| H9 | «Больше извлечённых чанков = лучше ответ» | Liu et al., TACL 2024 | Позиционное смещение: релевантный чанк в середине окна теряется; рост k повышает recall и *снижает* точность ответа | Э2 |
| H10 | «Проблема RAG — в retrieval» | Barnett et al., 2024, [arXiv:2401.05856](https://arxiv.org/abs/2401.05856) («Seven Failure Points of RAG») | Значительная часть отказов лежит вне retrieval: в корпусе, в извлечении из PDF, в формате ответа | Э2 |
| H11 | «Reranker всегда улучшает» | E7→E8 (§3.3) | Reranker не может вернуть то, чего нет в кандидатах; при низком recall@k он лишь переставляет мусор | Э2 |
| H12 | «Эмбеддинг понимает смысл» | Steck et al., WWW '24; Thakur et al., NeurIPS 2021 (BEIR: провалы zero-shot переноса) | Эмбеддинг — координаты в пространстве обучения модели; вне домена обучения «смысл» не переносится | Э2 |
| H13 | «Semantic chunking окупается» | Практика индустрии vs отсутствие сильных публичных подтверждений на разнородных корпусах | Дорогой и модный приём с неочевидным выигрышем против структурного чанкинга | Э2 |
| H14 | «Hybrid (BM25 + dense) — компромисс для неуверенных» | Contextual Retrieval (Anthropic, 2024); RRF (Cormack et al., SIGIR 2009) | Наоборот: гибрид — это признание, что лексический и семантический сигналы **ортогональны**, а не хуже/лучше | Э2 |
| H15 | «LLM-as-judge объективнее человека» | RAGAS (Es et al., EACL 2024); ARES (Saad-Falcon et al., NAACL 2024, [arXiv:2311.09476](https://arxiv.org/abs/2311.09476)) | Судья наследует смещения модели (позиционное, длина, self-preference); §15.1.6 v0.4 требует «судья — вход, не решение» | Э2/Э3 |
| H16 | «Graph RAG — универсальное улучшение» | Edge et al., 2024, [arXiv:2404.16130](https://arxiv.org/abs/2404.16130) | Авторы заявляют выигрыш на **глобальных summary-запросах**; цена — построение графа. На factual-запросах — оверинжиниринг | Э2/Э3 |
| H17 | «Self-RAG/CRAG — это про качество» | Asai et al., ICLR 2024, [arXiv:2310.11511](https://arxiv.org/abs/2310.11511); Yan et al., 2024, [arXiv:2401.15884](https://arxiv.org/abs/2401.15884) | Это про **право не отвечать** и про контур коррекции, то есть про D7/D9 — управляемость, а не точность | Э2 |
| H18 | «Векторный поиск заменяет структурированный запрос» | Text-to-SQL/BIRD, NeurIPS 2023, [arXiv:2305.03111](https://arxiv.org/abs/2305.03111) | Для прайсов/таблиц с агрегацией («самый дешёвый тариф») retrieval **не может** дать верный ответ: агрегация — не поиск | Э2 |
| H19 | «Golden Set — примитив, RAG — зрелость» | §15.1.4 v0.4; каскад (H2) | Обратная сторона: Golden Set даёт детерминизм и аудируемость, которых RAG не даёт. Это не лестница зрелости, а разные контракты качества | Э2/Э3 |
| H20 | «Retrieval — инфраструктура, не память агента» | §2.1, §15.1.4 v0.4; MemGPT (Packer et al., 2023, [arXiv:2310.08560](https://arxiv.org/abs/2310.08560)) | Если агент сам решает, что извлечь и что записать, retrieval становится **механизмом памяти**, а не сервисом под ней | Э3 |

---

> **Конец Этапа 1.** Ниже — Этап 2 ([Таксономия §1](20-taxonomy.md#1-таксономия-базы-знаний)–[Решения §6](30-decision-framework.md#6-readiness-levels-бз-l0l5), теоретический блок). Разделы [Практика §1](40-practice-and-cases.md#1-индустриальные-кейсы)–[Открытые вопросы §6](50-open-research.md#6-самоаудит)
> (практика, тренды, ранжирование, источники, самоаудит; Этап 3) следуют ниже.
> PR остаётся в статусе draft до подтверждения фаундером (issue #418).

---
