---
status: draft
version: 0.1
updated: 2026-09-10
temperature: 0.3
type: research
context: [ba, taxonomy, process, open-questions, backlog, portability, issue-571]
method: gap-listing
scope: mango-only
source: "https://github.com/G-Ivan-A/hybrid-Intelligence-lab/issues/571"
based_on:
  - research/ba-requirements/ba-process-taxonomy/20-taxonomy.md
related_issues:
  - "https://github.com/G-Ivan-A/hybrid-Intelligence-lab/issues/571"
---

# Незакрытое: вопросы, гипотезы и бэклог

## 1. Переносимость — гипотеза, а не свойство

Уровни `L2` и `L3` выведены из предметной области Манго КК и среды GigaCode.
Утверждение «таксономия применима к другой компании» не проверялось и не
делается. Проверяемая формулировка гипотезы: *если `L0` и `L1` унаследованы из
BABOK, то при смене предметной области меняются только `L2` и `L3`, а форма
уровней и правила `LV-1`–`LV-3`, `DL-1`–`DL-4`, `PR-1`–`PR-5` сохраняются.*
Способ проверки — применение к одному внешнему домену с фиксацией доли правил,
потребовавших изменения.

## 2. Открытые вопросы

| # | Вопрос | Почему не закрыт здесь |
| --- | --- | --- |
| 1 | Совместим ли базис с IREB CPRE и SWEBOK? | взят один свод знаний; сопоставление трёх сводов — самостоятельная работа |
| 2 | Каков порог значимости неоднозначности, направляющий работу в `P-02`? | порог задаётся эмпирикой первых прогонов; сейчас объявлен как параметр маршрута, а не как число |
| 3 | Кто владеет каталогом значений `L3` при росте: Хаб или спица? | `T-5` отдаёт спице каталог значений предметной области, но навык — исполняемый артефакт среды, и его дом обсуждается вместе с домом Execution Package |
| 4 | Достаточно ли двух итераций уточнения (`RT-4`)? | предел назначен проектно; данных о распределении числа итераций нет |
| 5 | Нужен ли отдельный процесс приоритизации требований (`RLCM` `Prioritize Requirements`)? | в практике КК приоритизация наблюдалась как атрибут элемента, а не как работа с собственным выходом; при появлении контрпримера открывается `T-2` |

## 3. Что вынесено в бэклог

| Задача | Предмет |
| --- | --- |
| `B-154` | Перенос дома мета-модели и таксономий в `projects/<направление>/` вслед за правилом 2 `<project_specific_rules>` |
| `B-155` | Стандарт мета-модели: перевод исследовательских правил в норму `standards/` после первого вертикального среза |
| `B-156` | Сбор эмпирики по `MP-1`–`MP-6` на первых прогонах и пересмотр ярусов `G1`–`G7` в §5 [`20-taxonomy.md`](https://github.com/G-Ivan-A/hybrid-Intelligence-lab/blob/main/research/ba-requirements/ba-process-taxonomy/20-taxonomy.md) |
| `B-157` | Сопоставление `L0` с IREB CPRE и SWEBOK, проверка вопроса 1 |

## 4. Что сознательно не сделано

- **Не переписаны модули `M0`–`M4`.** Их согласование с новой таксономией
  выполнено только там, где старое утверждение прямо противоречит новому
  (мета-модель); сплошной рефактор исторических текстов запрещён правилом
  «не переписывать чужие артефакты вместо создания новой версии».
- **Не созданы файлы Execution Package.** Модуль объявляет, что навык — единица
  компиляции (`SB-6`), но не компилирует его.
- **Не заведён каталог значений продуктовых классов.** Он остаётся в споке.
